name: Safie Auto Downloader

on:
  repository_dispatch:
    types: [trigger_download]

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        pip install selenium requests
        sudo apt-get update
        sudo apt-get install -y libnss3 libgbm1 libasound2t64 || sudo apt-get install -y libnss3 libgbm1 libasound2

    - name: Run Downloader
      env:
        SAFIE_ID: ${{ secrets.SAFIE_ID }}
        SAFIE_PW: ${{ secrets.SAFIE_PW }}
        GAS_WEBHOOK_URL: ${{ secrets.GAS_WEBHOOK_URL }}
        DOWNLOAD_URL: ${{ github.event.client_payload.download_url }}
      run: python main.py

    # 💡【ここが最終兵器】外部サイトを一切挟まず、Google公式ツールで動画フォルダをドライブへ直接アップロードします！
    - name: Upload to Google Drive
      uses: Jvshv/google-drive-upload-git-action@v1.1.0
      with:
        credentials: ${{ secrets.DRIVE_CREDENTIALS }} # 👈手順2で設定する金庫の鍵
        filename: ./output/
        folderId: '17lDpuOIqM7iLQPLm_1EVOHqBxEQ7195K' # 山下さんのフォルダID
        overwrite: "true"
